/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2025 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */


#include <stdint.h>


int main(void)
{
	uint32_t *pClkCtrlReg =  (uint32_t*)0x40023830;
	uint32_t *pPortDModeReg =  (uint32_t*)0x40020C00;
	uint32_t *pPortDOutReg =  (uint32_t*)0x40020C14;

	// enable the clock for GPIO D Peripherial set the the 3rd bit position
	*pClkCtrlReg |= (1 << 3); // toggling the 3rd bit using shift operator

	// Configure the mode of the pin as an output

	*pPortDModeReg &= ~(3 << 24); // first clear the bit using shift operator


	*pPortDModeReg |= (1 << 24); // then write into it using shift operator


	//set the 12th bit of the output data register to make I/O pin-12 as High


    /* Loop forever */


	for(;;){

		*pPortDOutReg |= (1 << 12); // setting data using shift operator
		for(uint32_t i = 0;i < 10000; i++ );

		*pPortDOutReg &= ~(1 << 12); // setting data using shift operator
		for(uint32_t i = 0;i < 10000; i++ );

	}
}
